@model Assignment2.Models.ViewModels.ClientSubscriptionsViewModel
@{
    ViewData["Title"] = "Edit Subscriptions";
}

<h1>Edit Subscriptions</h1>


<div>
    <h4>Client</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
           Full Name
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Client.FullName)
        </dd>
    </dl>


@*    </table>
            <tr>
                <td>
                    @item.NewsBoard.Title
                </td>
            </tr>

    </table>*@
    
    <table class="table">
@*    @if(Model.Subscriptions.c){
         <tr>
            <td>
                Not Subscribed
            </td>
         </tr>
    }*@
    
    @foreach (var item in Model.Subscriptions)
    {
        var regOrUnreg = "Register";
        if(item.IsMember == true)
        {
            regOrUnreg = "Unregister";
        }
        <tr>
            
            <td>
                 @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                <a asp-action="EditSubscriptions" asp-route-id="@Model.Client.Id" asp-route-newsboardId="@item.NewsBoardId">@regOrUnreg</a>
            </td>
        </tr>
    }
    
    </table>
</div>
<div>
    <a asp-action="Index">Back to List</a>
</div>
